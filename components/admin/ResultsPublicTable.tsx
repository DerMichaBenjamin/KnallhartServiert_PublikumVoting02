import type { AdminRoundSummary } from '@/lib/releaseVotingShared';
import { buildAudienceResults } from '@/lib/combinedVotingResults';

export default function ResultsPublicTable({ summary }: { summary: AdminRoundSummary }) {
  const rows = buildAudienceResults(summary.leaderboard, summary.countedVotes);
  return <section className="ks-card" id="public-results"><div className="ks-section-heading"><div><span className="ks-section-kicker">Publikums-Voting</span><h2>Gesamtauswertung</h2><p>„Publikumspunkte“ sind die tatsächlich summierten Punkte der Publikumsstimmen; die Spalte „12–1 Punkte“ zeigt die daraus abgeleitete Jury-Stimme.</p></div></div><div className="ks-results-split"><div className="ks-table-scroll"><table className="ks-table results"><thead><tr><th>Platz</th><th>Song</th><th>Künstler</th><th>Publikumspunkte</th><th>Gewählt</th><th>Anteil</th><th>12–1 Punkte</th></tr></thead><tbody>{rows.map((row) => <tr key={row.song.id}><td><span className="ks-rank-pill">{row.rank}</span></td><td><strong>{row.song.title}</strong></td><td>{row.song.artist}</td><td>{row.total}</td><td>{row.count}</td><td>{summary.countedVotes ? `${((row.count / summary.countedVotes) * 100).toFixed(1)} %` : '—'}</td><td><strong>{row.audiencePoints}</strong></td></tr>)}{!rows.length && <tr><td colSpan={7} className="ks-table-empty">Noch keine gewertete Publikumsrangliste vorhanden.</td></tr>}</tbody></table></div><aside className="ks-results-summary"><h3>Stimmenstatus</h3><dl><div><dt>Teilnehmer insgesamt</dt><dd>{summary.totalVotes}</dd></div><div className="success"><dt>Gewertet</dt><dd>{summary.countedVotes}</dd></div><div><dt>Bestätigt</dt><dd>{summary.confirmedVotes}</dd></div><div className="warning"><dt>Nicht bestätigt</dt><dd>{summary.unverifiedVotes}</dd></div><div className="warning"><dt>In Prüfung</dt><dd>{summary.reviewVotes}</dd></div><div className="danger"><dt>Ausgeschlossen</dt><dd>{summary.excludedVotes}</dd></div></dl><p>Nur bestätigte und aktuell gewertete Stimmen fließen in diese Rangliste ein.</p></aside></div></section>;
}
