'use client';import { useState } from 'react';
export default function LoginForm(){const[p,setP]=useState(''),[e,setE]=useState('');async function s(ev:React.FormEvent){ev.preventDefault();setE('');const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:p})});if(r.ok) location.href='/admin/release-voting'; else setE('Login fehlgeschlagen.');}return <form onSubmit={s} className="form"><input type="password" value={p} onChange={ev=>setP(ev.target.value)} placeholder="Admin-Passwort"/><button className="submit">Einloggen</button>{e&&<p className="error">{e}</p>}</form>}
