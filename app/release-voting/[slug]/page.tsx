import Header from '@/components/Header';import SpotifySidebar from '@/components/SpotifySidebar';import PublicVotingForm from '@/components/PublicVotingForm';import { buildLeaderboard, buildZonk, getRoundBySlug, getSongs, getVerifiedVotes } from '@/lib/releaseVoting';import Link from 'next/link';
export const dynamic='force-dynamic';
function fmt(v:string|null){return v?new Date(v).toLocaleString('de-DE',{dateStyle:'short',timeStyle:'short'}):'offen'}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const round=await getRoundBySlug(slug);if(!round)return <main className="public-shell"><Header/><h1>Abstimmung nicht gefunden</h1></main>;const songs=await getSongs(round.id);const now=Date.now();const isLive=round.status==='live'&&(!round.starts_at||Date.parse(round.starts_at)<=now)&&(!round.ends_at||Date.parse(round.ends_at)>=now);const {votes,items}=await getVerifiedVotes(round.id);const board=buildLeaderboard(songs,votes,items);const zonk=buildZonk(songs,votes).filter(z=>z.count>0);return <main className="voting-page"><SpotifySidebar playlistId={round.spotify_playlist_id}/><section className="voting-main"><Header/><h1>Knallhart serviert<br/><span>Publikums-Voting</span></h1><p>Wähle deine 12 Favoriten und bestätige deine Stimme per E-Mail.</p><div className="status-grid"><div className="status-card"><small>Dein Voting-Status</small><b>{isLive?'Noch keine Auswahl':'Beendet'}</b></div><div className="status-card"><small>Abstimmungszeitraum</small><b>{fmt(round.starts_at)} – {fmt(round.ends_at)}</b></div></div>{isLive?<PublicVotingForm roundId={round.id} roundTitle={round.title} placesCount={round.places_count} songs={songs}/>:<section className="card"><h2>Endstand User-Voting</h2><p>Gezählt werden nur per E-Mail bestätigte Stimmen.</p><table><thead><tr><th>#</th><th>Song</th><th>Gesamt</th><th>Ø Punkte</th><th>Gewählt</th></tr></thead><tbody>{board.map((r,i)=><tr key={r.song.id}><td>{i+1}</td><td><b>{r.song.title}</b><br/><small>{r.song.artist}</small></td><td>{r.total}</td><td>{r.avg.toFixed(2)}</td><td>{r.count}</td></tr>)}</tbody></table><div className="zonk-results"><h3>Z-O-N-K – Song der Woche</h3>{zonk.length? <ol>{zonk.map(z=><li key={z.song.id}>{z.song.title} — {z.song.artist}: {z.count}</li>)}</ol>:<p>Noch keine Z-O-N-K-Stimmen vorhanden.</p>}</div><Link className="btn" href="/release-voting">Zur aktuellen Abstimmung</Link></section>}<footer><Link href="/impressum">Impressum</Link></footer></section></main>}
