import { NextResponse } from 'next/server';
export async function POST(req:Request){const form=await req.formData();const name=String(form.get('name')||'');const email=String(form.get('email')||'');const message=String(form.get('message')||'');const apiKey=process.env.RESEND_API_KEY?.trim();const from=process.env.RESEND_FROM_EMAIL?.trim();if(!apiKey||!from)return NextResponse.json({ok:false,error:'Mail nicht konfiguriert.'},{status:500});const res=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${apiKey}`,'Content-Type':'application/json'},body:JSON.stringify({from:`Knallhart serviert Kontakt <${from}>`,to:['info@michabenjamin.de'],reply_to:email,subject:'Kontaktformular Knallhart serviert Voting',text:`Name: ${name}\nE-Mail: ${email}\n\n${message}`})});if(!res.ok)return NextResponse.json({ok:false,error:await res.text()},{status:500});return NextResponse.redirect(new URL('/kontakt?sent=1',req.url));}
