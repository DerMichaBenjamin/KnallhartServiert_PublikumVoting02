import { NextRequest, NextResponse } from 'next/server';import { ensureAdminRequest } from '@/lib/adminAuth';import { getSupabaseAdminClient } from '@/lib/supabaseAdmin';import { parseSongList } from '@/lib/releaseVoting';
export async function POST(req:NextRequest){const auth=ensureAdminRequest(req);if(!auth.ok)return NextResponse.json({ok:false,error:auth.error},{status:401});try{const b=await req.json();const sb=getSupabaseAdminClient();if(!sb)throw new Error('Supabase fehlt');const parsed=parseSongList(String(b.songsText||''));const rows=parsed.map((s,i)=>({round_id:b.roundId,title:s.title,artist:s.artist,sort_order:1000+i}));if(rows.length){const {error}=await sb.from('release_voting_songs').insert(rows);if(error)throw error;}return NextResponse.json({ok:true});}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:'Fehler'},{status:500})}}
